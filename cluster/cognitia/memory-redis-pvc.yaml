apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: memory-redis-data
  namespace: cognitia
  labels:
    app.kubernetes.io/name: memory-redis
    app.kubernetes.io/component: cache
    app.kubernetes.io/part-of: cognitia-memory
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-csi
