apiVersion: apps/v1
kind: Deployment
metadata:
  name: cognitia-web
  namespace: cognitia
  labels:
    app.kubernetes.io/name: cognitia-web
    app.kubernetes.io/component: web
    app.kubernetes.io/part-of: cognitia
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: cognitia-web
  template:
    metadata:
      annotations:
        cognitia.io/rebuild: "2025-12-25-web-logoutfix"
      labels:
        app.kubernetes.io/name: cognitia-web
        app.kubernetes.io/component: web
        app.kubernetes.io/part-of: cognitia
    spec:
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: cognitia-web
          image: fabbricca/cognitia-web:logoutfix-20251225-0032
          imagePullPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 2
            periodSeconds: 10
      restartPolicy: Always
