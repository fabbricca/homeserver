apiVersion: v1
kind: Service
metadata:
  name: cognitia-redis
  namespace: cognitia
  labels:
    app.kubernetes.io/name: cognitia-redis
    app.kubernetes.io/component: cache
    app.kubernetes.io/part-of: cognitia
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    app.kubernetes.io/name: cognitia-redis
