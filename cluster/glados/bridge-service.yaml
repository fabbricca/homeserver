apiVersion: v1
kind: Service
metadata:
  name: glados-websocket-bridge
  namespace: glados
spec:
  selector:
    app: glados-websocket-bridge
  ports:
  - protocol: TCP
    port: 8765
    targetPort: 8765
