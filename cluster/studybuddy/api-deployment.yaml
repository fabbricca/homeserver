apiVersion: apps/v1
kind: Deployment
metadata:
  name: studybuddy-api
  namespace: studybuddy
  labels:
    app.kubernetes.io/name: studybuddy-api
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: studybuddy
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: studybuddy-api
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/restartedAt: "2026-03-31T19:18:35Z"
      labels:
        app.kubernetes.io/name: studybuddy-api
        app.kubernetes.io/component: api
        app.kubernetes.io/part-of: studybuddy
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: studybuddy-api
          # Build from services/api/Dockerfile (monorepo context):
          #   docker build -t fabbricca/studybuddy-api:latest -f services/api/Dockerfile .
          #   docker push fabbricca/studybuddy-api:latest
          image: fabbricca/studybuddy-api@sha256:a4ea55e260faa73152ba97411197816e6a4196e0dd2497e7c51400c1fabecca2
          imagePullPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: false
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          envFrom:
            - configMapRef:
                name: studybuddy-api-config
            - secretRef:
                name: studybuddy-api-secret
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
      restartPolicy: Always
